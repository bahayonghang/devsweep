# Software Mode Parent Acceptance Review

Date: 2026-08-31
Reviewer role: coordination-parent acceptance reviewer (umbrella `08-29-software-mode`, review-only; no sub-agents spawned, no product files touched, no commits).
Review basis: `HEAD = a8fd585334532c91214d37a42cc0dd7aa266c78d` (archive commit of the last leaf). Archived child evidence under `.trellis/tasks/archive/2026-08/` was traced clause by clause; product state was independently grepped at HEAD.

## Reviewed leaf product commits and archived task paths

| Child | Product commit | Archive commit | Archived path |
| --- | --- | --- | --- |
| `08-29-software-inventory-plan` | `b1d0ed8` (`b1d0ed85e97feb14d13ccfeb1ee27ac5ea26f4dd`, feat(software): Software V1 inventory/preview domain) | `b650128` | `.trellis/tasks/archive/2026-08/08-29-software-inventory-plan/` |
| `08-29-software-execution-audit` | `836a916` (`836a916bee50ef176aafd1994736dd20a0f4219f`, feat(software): MSIX execution + durable audit) | `ed0803a` | `.trellis/tasks/archive/2026-08/08-29-software-execution-audit/` |
| `08-29-software-cli-tui-desktop-native` | `2337cd5` (`2337cd511197303ed470b17fda97a75f0a037683`, feat(software): three-surface workbench + native evidence) | `a8fd585` | `.trellis/tasks/archive/2026-08/08-29-software-cli-tui-desktop-native/` |

Ordering verified from `git log --oneline`: each product commit is immediately followed by its independent `chore(task): archive` commit, in the mandated child order inventory/plan -> execution/audit -> presentation/native (R3 ordering). All three child `task.json` files carry `status: completed` and `parent: 08-29-software-mode`. No umbrella product-code commit exists; every product commit is child-tagged (`Agent-Task:` trailer). Archived status alone was not used — product state was verified at HEAD.

## Per-requirement verdicts

| Clause | Verdict | Evidence |
| --- | --- | --- |
| R1 (separate Software domain: tagged inventory -> saved plan -> preview digest -> current-user MSIX-only execution -> requery -> audit -> CLI/TUI/Desktop -> native no-UAC evidence) | PASS (with sanctioned UNVERIFIED: real-uninstall native scenarios only) | Inventory/plan: `crates/devsweep-core/src/software/` (arp/msi/msix/model/plan) in `b1d0ed8`; child-1 check report PASS + `independent-native-evidence-audit.json` (1,293 entries: 823 ARP / 291 MSI / 179 MSIX, 5 available + 3 partial sources, TTL 900,000 ms). Execution/audit: `software/execution/` (mod/audit/msix) in `836a916`; child-2 check PASS, `independent-static-audit.log` (durable `dispatch_started` with `sync_all`, journal replay graph, `recover_pending` identity-only requery, no redispatch). Presentation/native: `2337cd5` adds CLI renderer, `tui/modes/software/`, `desktop/src/modes/software/`, `desktop/src-tauri/src/software.rs`; native evidence: real 1,293-entry standard-user inventory, real VP9 plan/preview (digest `sha256:4fdcf3cb...ed46`, recomputed from artifact and matching), `native-20260831/` screenshots, AX tree, `integrity.txt` (medium integrity `S-1-16-8192`). Sanctioned UNVERIFIED: real `RemovePackageAsync` success/reboot/cancel/timeout/crash-restart/requery-denial scenarios — repo has no disposable current-user MSIX fixture and `implement.md` forbids installing/signing one; parked until a user-confirmed target, exercised downstream by `08-29-five-mode-native-integration`. |
| R2 (registry-only + every MSI context visible manual/unselectable; vendor uninstall strings, icons, fuzzy names, publishers, guessed leftovers never authority or plan data) | PASS | Closed 13-reason refusal matrix incl. `MsiExecutionNotSupportedV1`, `RegistryOnlyManual`, `ProtectedProduct`, `NoRemove`, `HiddenEntry`, `SystemOrUpdate`, `DependencyPackage`, `StubPackage`, `UnhealthyPackage`, `ConflictingIdentity`, `SourceIncomplete`, `UnsupportedSource` (`crates/devsweep-core/src/software/model.rs:135-149`). Child-1 check: all 291 native MSI entries manual, no non-MSIX selectable, no fuzzy merging (order-independent duplicate merge fix applied in check). Native artifacts: 1,207 manual / 86 selectable; the only native plan targets an MSIX package full name. Redaction verified independently: `UninstallString`/`QuietUninstallString`/`DisplayIcon` occur only inside `#[cfg(test)]` hostile fixtures/redaction assertions (inventory.rs:236+, execution.rs:294, audit.rs:945+ in test modules); native preview JSON scan found no command strings, `argv`, or `installed_path`; serialized plan payload is exactly five fields (verified in `software-plan-vp9.json`). |
| R3 (inventory/plan before execution/audit before presentation/native; umbrella owns handoffs, gates, fixtures, rollback only) | PASS | Commit/archive ordering above (07:36 -> 08:46 -> 10:56 product commits, each archived before the next leaf started). Umbrella owns this acceptance review and the contract handoffs; no umbrella-authored product change. Integration note (adjudicated, not drift): the execution child additively extended the preview DTO (`SoftwarePreviewV1.irreversible`, `SoftwarePreviewItemV1.identity`) to carry exact identity for execution revalidation and explicit irreversibility — additive, safety-positive, digest recomputed over the new shape, child-1 frozen tests unchanged and passing inside `just ci` (412 tests, `independent-full-gates.log`), and child-3 presentation decoders/native artifacts all use the final schema. Inventory-entry and plan schemas are unchanged across children. |

## Per-AC verdicts

| Clause | Verdict | Evidence |
| --- | --- | --- |
| AC1 (hostile inventory, identity/fingerprint, stale preview, all-MSI/manual + protected refusal, MSIX dispatch, cancellation, reboot, closed five-terminal outcome/restart recovery, partial-source evidence, audit, no-UAC) | PASS (with sanctioned UNVERIFIED: real-uninstall native scenarios; process-tree/no-UAC live probe) | Hostile inventory: child-1 AC1 (quoted commands, metacharacters, env vars, malformed GUIDs, duplicate names, huge strings, invalid UTF-16, denied keys cannot enter plans/logs/argv). Identity/fingerprint: child-1 AC2 with two fail-closed fixes (duplicate order-independence; fallible WinRT iteration marks partial instead of panicking). Stale preview: child-1 AC3 + native stale-authority run (exit 3, `invalid_software_authority`, zero stderr — `native-non-uninstall.log`). All-MSI/manual + protected: R2 evidence. Dispatch/cancel/reboot/five terminals/restart: child-2 AC2 — 35 core + 9 CLI tests, exhaustive 16-combination terminal matrix, concurrent single-flight proof (max one adapter), journal replay rejects terminal-after-`dispatch_started` without requery, restart requery never redispatches. Five-terminal table confirmed at HEAD: `SoftwareExecutionOutcome = { CanceledBeforeStart } + { Removed, RebootRequired, StillPresent, Failed, UnknownAfterDispatch }` (`execution/mod.rs:45-52`); `"partial"` rejected by serde wire test (`desktop/src-tauri/src/software.rs:338-339`) and TS union exclusion. Partial-source: truthful native envelope `outcome: partial` with three partial ARP partitions. Audit: child-2 R4 durability/redaction/version (`SoftwareAuditRecordV1` has identity/fingerprints/transitions only — no command, argv, path, localized text). No-UAC: no `runas`/elevation/`consent` references in any software code path (grepped at HEAD); `asInvoker`; medium integrity verified via `%WINDIR%\System32\whoami.exe`; process-tree probe UNVERIFIED (sandbox `Get-CimInstance Win32_Process` access denied — documented honestly, not promoted into a claim). |
| AC2 (same fixture -> matching CLI/TUI/Desktop identities, selection eligibility, reason codes, locale-invariant machine documents) | PASS | Child-3 independent check section 5: the real native CLI inventory (1,293 entries) satisfies the strict TS decoder invariants with 0 violations (eligibility pairing, selectable identity shape, closed reason set, strict last-used unknown, size-state shapes, duplicate-id rejection); key-union shapes exactly equal between native Rust serde output and the desktop fixture. TUI reducer/component tests consume the same fixtures; manual rows render `[-]` and never toggle; selection requires `selectable()` = Selectable + EligibleCurrentUserMsix + CurrentUser + MSIX. Desktop bridge enforces operation-id echo; decoders are closed-world `exact()` with plan==preview cross-check. Locale-invariant machine documents asserted by tests (`software_inventory_json_is_locale_invariant_and_preserves_partial_evidence`, `preview_machine_document_is_locale_invariant_and_irreversible`); bilingual renders in en/zh-CN at TestBackend 100 columns and in native captures. |
| AC3 (no plan/audit/log contains ARP uninstall command strings or guessed leftovers; no Software path constructs CleanupPlan or requests elevation) | PASS | Independent greps at HEAD: zero `CleanupPlan` references and zero `runas`/elevat/consent/ShellExecute references in `crates/devsweep-core/src/software/`, `crates/devsweep-cli/src/application/commands/software/`, `desktop/src-tauri/src/software.rs`. `ValidatedSoftwareAction` is opaque, non-serializable, private-fielded; `RemovePackageAsync` is called only by the private `RealMsixAdapter` with `package_full_name()`; executor-owned live provider replaced the caller-supplied inventory (check-fixed). Plan payload = exactly five frozen fields; preview items = exactly `id/identity/action_class/scope/eligibility/strategy_token`; audit schema has no raw output/command/argv/env/path/partial field; native artifacts scan clean. Permanent delete remains disabled; scanner/model layers create plans only. |

## Cross-child integration checks

- Frozen CLI grammar untouched: `git log aac5a03..HEAD -- crates/devsweep-cli/src/application/cli.rs` is empty; all three product commits leave the parser file unmodified; child-2/child-3 checkers independently ran `git diff --exit-code` on it (`independent-static-audit.log`, `independent-cli-grammar-untouched.log`).
- Software DTO stability: inventory-entry and plan schemas unchanged across children; the only preview-DTO change is the execution child's additive `irreversible`/`identity` extension (adjudicated under R3 above — no identity/eligibility semantic change, no authority expansion, child-1 tests pass unchanged, final schemas consistent across all three surfaces and native artifacts). Outcome/audit schemas are owned by the execution child per design; presentation consumed them without core changes (no `crates/devsweep-core` paths in `2337cd5`).
- Cleanup authority: the only destructive path is `software_uninstall` -> `SoftwareExecutor::execute` behind preview + second confirmation + core digest/confirmation checks; no Software-to-CleanupPlan conversion exists; program/argv separation of existing cleanup commands untouched; no new dependency beyond the approved `windows =0.56.0` projection (child-1) and a benign `serde_json` addition to the desktop crate for wire tests.
- All-MSI/manual rule end to end: type-level closed reason set -> core inventory (all 291 native MSI manual) -> preview (only MSIX identity plannable) -> execution (MSI/registry/machine/protected/malformed rejected before adapter; compile-fail authority doctests) -> CLI/TUI/desktop (manual rows visible, disabled, aria-labelled, never toggle) -> fixtures (`all-msi-manual.json`).
- Last-used strictly unknown: closed V1 value `unknown/no_supported_exact_source`; wire test pins exactly two keys; every native entry reports it (1,293/1,293); TS decoder enforces the strict union; no InstallDate/InstalledDate/UserAssist/telemetry inference anywhere.
- Five terminals: closed enum + exhaustive fixture (`execution-five-terminal.json`) + serde/TS `partial` rejection + restart-audit fixture proving recovery to `unknown_after_dispatch` without redispatch; no `partial` execution view/fixture exists anywhere.
- No reversibility claims: preview carries `irreversible: true`; all surfaces state DevSweep cannot restore/reinstall ("from DevSweep's perspective"); component-tested in both locales; execution child static audit found no rollback/reinstall promise in production text.
- Registration: Software mode registered in the desktop shell (`App.tsx` adds the `software` route) only after native standard-user evidence was captured, per design.

## Commands, exit codes, and log paths (independent, per archived evidence)

- Child 1: `cargo test -p devsweep-core software` (14 passed, exit 0, `independent-core-software-tests.log`), `cargo test -p devsweep-cli software` (5 passed, exit 0, `independent-cli-software-tests.log`), `just ci` (exit 0, `independent-just-ci.log` tail `EXIT_CODE: 0`), `npm --prefix desktop run typecheck` (exit 0), `just release-archive` (exit 0, binary 3,900,416 bytes / zip 1,541,981 bytes within budgets, `independent-release-archive.log`, `independent-size-budget.json`), native host check (non-elevated, valid SID shape, no UAC observed, `independent-native-host-check.log`).
- Child 2: focused core/CLI software gates (exit 0, `independent-focused-gates.log`), `just ci` 412 tests (exit 0, `independent-full-gates.log`), frozen-parser/manifest diff checks (exit 0, `independent-static-audit.log`), native non-uninstall run: stale-authority exit 3 with zero stderr, no `consent.exe`, no orphaned process (`native-non-uninstall.log`; the discarded Git-bundled `whoami` deviation is recorded in-log).
- Child 3: `cargo test -p devsweep-cli software` (13 passed), `cargo test -p devsweep-cli tui::modes::software` (4 passed), `cargo test -p devsweep-desktop software` (2 passed), `cargo test -p devsweep-cli --lib` (140 passed), `just ci` (exit 0 after an in-scope clippy repair), `npm --prefix desktop run typecheck` (exit 0), checker-run `npm --prefix desktop run test` (22 files / 130 tests, exit 0, `independent-postfix-gates.log` tail `EXIT:0`), grammar-untouched diff (exit 0). Main-session logs audited and corroborated: `desktop-web-check-final.log` (lint/typecheck/vite build, 130/130), `desktop-build-main.log` (release Tauri build + NSIS bundle). Sandbox `spawn EPERM` failures and the supplementary shim suite (8 tests / 3 files) are documented in `gate-summary.md` without being passed off as the standard gate.

## Manual/native UNVERIFIED ledger (all sanctioned, none completion-required here)

1. Real current-user MSIX uninstall scenarios: success + exact-identity absence, reboot-required with absent/present requery, pre/post-dispatch cancellation, 120-second timeout + OS cancel + five-second grace, crash/restart reconciliation after durable `dispatch_started`, requery denial/unavailability, definitive failure or completion-with-still-present (execution + presentation children; awaiting a user-confirmed disposable target; `implement.md` forbids installing/signing a fixture; fake-adapter/integration coverage and native non-uninstall evidence exist in the meantime).
2. Process-tree / no-UAC live probe beyond verified medium integrity (sandbox `Win32_Process` access denied; recorded, not promoted).
3. Live interactive TUI keyboard capture (covered by TestBackend render + reducer/runtime tests).
4. Native captures of the second-confirmation dialog, reduced-motion, and high-contrast appearances (covered by desktop component tests + TUI TestBackend; boundary made explicit in `native-evidence.md`).
5. Free-space recovery and activity/last-used claims — prohibited by task documents, none made (not a gap).

All five are documented in the children's `native-evidence.md` / check reports with the same boundary language; nothing unsanctioned was silently dropped. Per `implement.md`, overall PASS requires no completion-required UNVERIFIED: none of the above is completion-required for this umbrella, and the umbrella deliberately remains active (archival deferred until `08-29-five-mode-native-integration` passes, which owns the deferred native real-uninstall scenarios).

## Overall verdict

**PASS**

No defect requires returning work to a child. The sanctioned UNVERIFIED boundary is consistently documented across the execution and presentation children and is enforced by the no-fixture rule in `implement.md`; per the umbrella's own gate this does not block PASS. This umbrella stays active after PASS; archive only after `08-29-five-mode-native-integration` passes.

Reviewer note (context, not a defect): the working tree contains post-archive local changes from other in-progress tasks (`README.md`, `justfile`, `.trellis/.gitignore`, and a one-line `crates/devsweep-cli/tests/cli_contract.rs` staged-mode probe swap belonging to the optimize task). None of these are part of this parent's reviewed commits and none were touched by this review.
