STOPPED at the frozen CLI contract boundary; task is not complete.

Implemented Steps 1–2:

- Added the approved Windows-only `windows = "=0.56.0"` dependency with exactly five named features. `Cargo.lock` adds only the direct core dependency reference.
- Implemented Software V1 core:
  - HKCU/HKLM ARP inventory across 32/64-bit registry views using allowlisted values only.
  - Current-user managed/unmanaged and machine MSI identities; all remain manual.
  - Dedicated joined MTA worker using the current-process SID and `FindPackagesByUserSecurityId`.
  - Exact identities/deduplication, ordered refusal matrix, checked size evidence, truthful unknown last-used evidence, immutable fingerprints, inventory expiry, untrusted exact-ID plans, live stale revalidation, opaque strategy tokens, and preview digest validation.
  - No CleanupPlan creation, shell/process fallback, vendor command/icon reads, other-user enumeration, UAC, or serialized installed paths.

Step 1 size gate passed:

- Binary: 3,487,232 → 3,487,232 bytes; delta 0 bytes; threshold 5,242,880.
- Zip: 1,391,501 → 1,392,738 bytes; delta 1,237 bytes; threshold 2,097,152.
- Core dependency tree contains only `windows 0.56.0`. Workspace `Cargo.lock` already contained unrelated `windows 0.61.3`; this task introduced no new package/version block.
- License: `MIT OR Apache-2.0`; Rust 1.62; cached archive 10,807,828 bytes.

Final passing gates:

- `cargo check -p devsweep-core --all-targets` — exit 0.
- `cargo test -p devsweep-core software::windows` — exit 0; 1 passed.
- `cargo test -p devsweep-core software::inventory` — exit 0; 5 passed.
- `cargo test -p devsweep-core software::plan` — exit 0; 3 passed.
- `cargo fmt --all -- --check` — exit 0.
- Evidence JSON/JSONL integrity check — exit 0.

Recorded corrected iterations:

- Initial formatting check exited 1; `cargo fmt --all` corrected it.
- One plan-hardening run exited 1 because the test’s simulated live inventory retained an obsolete fingerprint. Recomputing the fixture fingerprint allowed header-integrity validation before the intended stale-selection assertion; final run passed.
- An initial broad Software test rejected the required source code `msix_installed_path`; the assertion was corrected to reject an actual serialized `path` field.

Blocking condition:

`crates/devsweep-cli/src/application/commands/mod.rs` routes only `clean.*` and `analyze.*`; the promised predeclared Software dispatch call is absent. Existing behavior is:

```text
{"command":"software.inventory","data":null,"error":{"code":"mode_unavailable","message":null},"outcome":"failed","schema_version":1,"warnings":[]}
PROCESS_EXIT_CODE: 4
```

Wiring Software would require editing the frozen command-dispatch root, prohibited by the task contract and Step 3 stop rule. Therefore I did not modify CLI/parser/presentation/catalogues and did not run CLI Software tests, `git diff --check`, `just ci`, or native Step 3 screenshots/evidence.

Diagnostic deviations:

- First `cargo run` omitted `--bin` and exited 1:

```text
error: `cargo run` could not determine which binary to run. Use the `--bin` option to specify a binary, or the `default-run` manifest key.
available binaries: devsweep, process_fixture
```

- One unrestricted metadata attempt emitted:

```text
error: failed to download `dbus v0.9.12`

Caused by:
  attempting to make an HTTP request, but --offline was specified
```

The Windows-filtered offline metadata command subsequently exited 0.

Files changed:

- `Cargo.lock`
- `crates/devsweep-core/Cargo.toml`
- `crates/devsweep-core/src/lib.rs`
- `crates/devsweep-core/src/software/{mod,model,arp,msi,msix,plan,tests}.rs`
- `.trellis/tasks/08-29-software-inventory-plan/implement.jsonl`
- Evidence under `.trellis/tasks/08-29-software-inventory-plan/evidence/`

Primary evidence:

- `step1-baseline.json`
- `step1-size-budget.json`
- `step1-cargo-tree-windows.log`
- `windows-binding-license.json`
- `core-windows.log`
- `core-inventory.log`
- `core-plan.log`
- `core-check.log`
- `source-safety-audit.log`
- `step3-frozen-dispatch-blocker.log`
- `implementation-status.json`

No commit or push was performed.