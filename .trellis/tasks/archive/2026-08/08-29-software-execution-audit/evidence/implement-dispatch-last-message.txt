Implementation is complete. No commits, pushes, dependency additions, elevation, vendor commands, installs, signing, or package removals were performed.

## Implementation summary

1. Validation and audit

- Added opaque, non-deserializable `ValidatedSoftwareAction`; construction requires live preview revalidation and an exact eligible current-user MSIX identity.
- Added type-level and runtime coverage proving MSI, registry-only, machine, other-user, protected, stale, and malformed identities cannot reach an adapter.
- Added locked, versioned, redacted JSONL audit records with durable pre-side-effect `dispatch_started`, monotonic transitions, stable codes, `flush`/`sync_all`, and exactly one terminal state.
- Added fail-closed startup recovery using exact identity-only requery. Recovery never retries removal.
- Digest and audit-lock failures occur before adapter invocation.

2. Exact MSIX execution

- Reused the inventory MTA worker for `PackageManager::RemovePackageAsync` with the exact `PackageFullName`.
- Enforced process-level and audit-lock single-flight execution.
- Added the 120-second monitor, pre-dispatch cancellation, one post-dispatch OS cancellation request, five-second grace period, and absolute 0/2/10-second exact-identity requeries.
- Implemented the closed post-dispatch terminal set: `removed`, `reboot_required`, `still_present`, `failed`, and `unknown_after_dispatch`.
- Added crash-before/after-dispatch, adapter-unfinished, evidence-conflict, absent-with-reboot, present-with-failure/success, and no-redispatch recovery coverage.
- No result claims reversibility or `partial`.

3. CLI

- Connected frozen `software preview` and `software uninstall` dispatch to the owned handler.
- Preview performs live current-user inventory revalidation and emits the exact tagged identity, digest, and irreversible status.
- Uninstall requires confirmation and executes only validated current-user MSIX actions.
- Added hostile-plan and locale-invariant machine-document coverage.

4. Native evidence

- Ran the real Windows preview binary under medium integrity.
- Observed no UAC process and no remaining product-owned child process after exit.
- Verified the expected stale-authority result: exit `3`, outcome `failed`, stable code `invalid_software_authority`, and empty stderr.
- Verified native audit durability, redaction, locking, and unknown-version refusal.
- Real-package uninstall scenarios remain explicitly `UNVERIFIED`.

## Validation

All required gates passed:

- `cargo test -p devsweep-core software::execution` — exit `0`, 13 passed.
- `cargo test -p devsweep-core software::audit` — exit `0`, 4 passed.
- `cargo test -p devsweep-core software::execution::msix` — exit `0`, 2 passed.
- `cargo test -p devsweep-core software` — exit `0`, 31 passed.
- `cargo test -p devsweep-cli software` — exit `0`, 8 passed.
- `cargo fmt --all -- --check` — exit `0`.
- `git diff --check` — exit `0`; only pre-existing/worktree LF-to-CRLF warnings.
- `just ci` — exit `0`.
  - 135 CLI unit tests passed.
  - 12 CLI contract tests passed.
  - 231 core tests passed.
  - 1 public API test passed.
  - 28 desktop tests passed and 2 were ignored.
  - Workspace clippy passed with warnings denied.
  - The only emitted warning was the Windows linker’s informational import-library message.
- Native audit exact test — exit `0`.
- `task.py validate` — exit `0`, with 8 implementation and 3 check entries.
- Additional focused core and CLI clippy runs with `-D warnings` — exit `0`.

Raw evidence:

- [focused-gates.log](D:/Documents/Code/Rust/Exp/devsweep/.trellis/tasks/08-29-software-execution-audit/evidence/focused-gates.log)
- [just-ci.log](D:/Documents/Code/Rust/Exp/devsweep/.trellis/tasks/08-29-software-execution-audit/evidence/just-ci.log)
- [native-non-uninstall.log](D:/Documents/Code/Rust/Exp/devsweep/.trellis/tasks/08-29-software-execution-audit/evidence/native-non-uninstall.log)
- [native-evidence.md](D:/Documents/Code/Rust/Exp/devsweep/.trellis/tasks/08-29-software-execution-audit/evidence/native-evidence.md)
- [native-preview-stale-plan.json](D:/Documents/Code/Rust/Exp/devsweep/.trellis/tasks/08-29-software-execution-audit/evidence/native-preview-stale-plan.json)

## Files changed

Core:

- `crates/devsweep-core/src/software/execution/mod.rs`
- `crates/devsweep-core/src/software/execution/audit.rs`
- `crates/devsweep-core/src/software/execution/msix.rs`
- `crates/devsweep-core/src/software/execution/tests.rs`
- `crates/devsweep-core/src/software/mod.rs`
- `crates/devsweep-core/src/software/model.rs`
- `crates/devsweep-core/src/software/msix.rs`
- `crates/devsweep-core/src/software/plan.rs`

CLI:

- `crates/devsweep-cli/src/application/commands/software/execution.rs`
- `crates/devsweep-cli/src/application/commands/software/inventory.rs`
- `crates/devsweep-cli/src/application/commands/software/mod.rs`

Task records and evidence:

- `.trellis/tasks/08-29-software-execution-audit/implement.jsonl`
- The five evidence files listed above.

Protected dirty files `.trellis/.gitignore`, `README.md`, and `justfile` were not edited. Existing unrelated changes in `cli_contract.rs`, the localization catalogs, and other task directories were preserved.

## Deviations encountered

- Initial CLI assertions accidentally matched hostile fixture-path text and the substring `reversible` inside `irreversible`. The assertions were corrected; final CLI tests pass.
- One exploratory Cargo invocation incorrectly supplied two test filters and returned:
  `error: unexpected argument 'software::audit' found`
  The required commands were then run separately and passed.
- Initial task validation referenced the dependency task at its obsolete active path. It was updated to the actual archived path; final validation passes.
- The first discarded native integrity probe resolved Git’s bundled `whoami` and failed with:
  `0 [main] whoami (83180) C:\Program Files\Git\usr\bin\whoami.exe: *** fatal error - CreateFileMapping S-1-5-21-2143440361-2401194771-2834292696-1001.1, Win32 error 5. Terminating.`
  The accepted probe explicitly invoked `%WINDIR%\System32\whoami.exe`.

## UNVERIFIED

Because the repository contains no disposable current-user MSIX fixture, the following real-uninstall native scenarios remain `UNVERIFIED` pending explicit user confirmation of a concrete disposable current-user `PackageFullName`:

- Real `RemovePackageAsync` success followed by exact-identity absence.
- Real reboot-required evidence with absent or present requery.
- Real pre-dispatch and post-dispatch cancellation.
- Real 120-second timeout, single OS cancellation request, and five-second grace.
- Real crash/restart recovery after durable `dispatch_started`.
- Real exact-identity requery denial or unavailability after dispatch.
- Real adapter failure or successful completion while the exact identity remains present.

No rollback, reinstall, restoration, or reversibility claim is made.